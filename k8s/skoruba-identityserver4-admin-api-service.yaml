apiVersion: v1
kind: Service
metadata:
  name: skoruba-identityserver4-admin-api
  namespace: skoruba-identityserver4
  labels:
    app: admin-api
spec:
  ports:
    - name: http
      port: 80
      protocol: TCP
      targetPort: 80
  selector:
    app: admin-api
